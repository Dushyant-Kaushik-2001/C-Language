#include <stdio.h>
#include <conio.h>
int main()
{
    int a, b;
    scanf("%d %d", &a, &b);                      // scanf mein jo do %d%d hain inke beech mein comma mhi lgega
    printf("a=%d b=%d", a, b);
    getch();
}
